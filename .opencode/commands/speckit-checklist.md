---
description: Generate a custom checklist for the current feature based on user requirements.
---

Load and follow the skill `speckit-checklist` defined in `.opencode/skills/speckit-checklist/SKILL.md`. First use the skill tool with id `speckit-checklist` to load its full instructions, then execute them.

User input: $ARGUMENTS
