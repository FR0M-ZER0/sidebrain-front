---
description: Execute the implementation planning workflow using the plan template to generate design artifacts.
---

Load and follow the skill `speckit-plan` defined in `.opencode/skills/speckit-plan/SKILL.md`. First use the skill tool with id `speckit-plan` to load its full instructions, then execute them.

User input: $ARGUMENTS
