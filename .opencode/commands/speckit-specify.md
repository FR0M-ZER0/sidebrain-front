---
description: Create or update the feature specification from a natural language feature description.
---

Load and follow the skill `speckit-specify` defined in `.opencode/skills/speckit-specify/SKILL.md`. First use the skill tool with id `speckit-specify` to load its full instructions, then execute them.

User input: $ARGUMENTS
