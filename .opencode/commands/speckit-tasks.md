---
description: Generate an actionable, dependency-ordered tasks.md for the feature based on available design artifacts.
---

Load and follow the skill `speckit-tasks` defined in `.opencode/skills/speckit-tasks/SKILL.md`. First use the skill tool with id `speckit-tasks` to load its full instructions, then execute them.

User input: $ARGUMENTS
